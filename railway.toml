[build]
builder = "nixpacks"

[deploy]
startCommand = "(/opt/venv/bin/alembic upgrade head || echo 'Alembic skipped') && /opt/venv/bin/uvicorn src.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
